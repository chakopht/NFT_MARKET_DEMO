import type { NextConfig } from 'next'
 
const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'coffee-eldest-monkey-667.mypinata.cloud',
      'ipfs.io',
      'gateway.pinata.cloud',
      'nftstorage.link',
    ],
  }
}
 
export default nextConfig